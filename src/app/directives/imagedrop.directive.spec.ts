import { ImagedropDirective } from './imagedrop.directive';

describe('ImagedropDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagedropDirective();
    expect(directive).toBeTruthy();
  });
});
